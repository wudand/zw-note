# 生效于 docker compose 构建（context = 仓库根目录）
.git
**/.DS_Store
**/.cursor
**/.vscode
**/.idea

# 含数据库密码等敏感信息，构建镜像不需要，避免混进 build context
docker/.env

# 前端
zw-note-frontend/node_modules
zw-note-frontend/dist
zw-note-frontend/*.log
zw-note-frontend/README.md
zw-note-frontend/README.en.md

# 后端
zw-note-backend/bin
zw-note-backend/**/*.md
# 注意：zw-note-backend/docs 是 swag 生成的 Go 包（docs.go），
# 被 bootstrap/router.go 以 `_ "zw-note-backend/docs"` 方式引入用于挂载
# Swagger UI，是编译期依赖，不能加入 ignore，否则会导致镜像构建失败
