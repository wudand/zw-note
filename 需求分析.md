# 知识笔记软件需求分析

## 一、项目概述
本项目为个人使用的知识笔记记录软件，基于 Vue 3 + TypeScript 开发，旨在为用户提供便捷的文档管理、Markdown 编辑及预览功能，帮助用户高效整理和查阅个人知识内容。

## 二、技术栈
- **前端框架**: Vue 3 + TypeScript
- **路由管理**: Vue Router 4
- **UI 组件库**: Element Plus
- **编辑器**: 自定义 Markdown 编辑器
- **布局组件**: Splitpanes（分割面板）
- **拖拽功能**: VueDraggable
- **构建工具**: Vite

## 三、核心功能模块

### 1. 文档列表页面

#### 1.1 文档展示
- **文档卡片布局**: 以卡片形式展示所有文档
- **文档信息展示**:
  - 文档缩略图（模拟文档外观，包含标题和作者信息）
  - 文档标题
  - 文档描述（支持多行显示，最多显示 2 行）
  - 文档作者
- **交互功能**:
  - 点击文档卡片可快速预览文档
  - 悬停效果（边框高亮、阴影效果）
- **响应式设计**: 支持桌面端、平板和移动端适配

#### 1.2 文档操作
- **创建新文档**:
  - 通过"创建新的文档"按钮打开创建弹窗
  - 创建表单包含：
    - 文档名称（必填，1-50 字符）
    - 作者（必填，1-20 字符）
    - 文档描述（可选，最多 200 字符）
  - 创建成功后自动跳转到编辑页面
- **编辑文档**: 点击"编辑"按钮跳转到文档编辑页面
- **预览文档**: 
  - 点击文档卡片或下拉菜单中的"查看"选项
  - 跳转到文档预览页面
- **设置操作**: 通过下拉菜单编辑文档基础信息（复用创建弹窗，已实现）
- **删除文档**: 通过下拉菜单删除文档，二次确认后调用接口移除（前端已实现，后端接口待补全）

#### 1.3 页面布局
- 顶部标题栏：显示"文档列表"标题和创建按钮
- 主内容区：文档卡片列表，支持滚动
- 响应式布局：移动端自动调整布局和字体大小

### 2. 文档编辑页面

#### 2.1 页面布局
- **分割面板布局**: 使用 Splitpanes 实现可调整的左右分栏
  - 左侧面板：文档目录（默认 15% 宽度）
  - 右侧面板：Markdown 编辑器（剩余宽度）
  - 支持拖拽调整面板宽度

#### 2.2 文档目录组件
- **目录展示**:
  - 树形结构展示文档目录
  - 支持多级目录（父级和子级）
  - 支持展开/折叠父级目录
  - 高亮当前激活的目录项
- **目录编辑功能**（编辑模式下）:
  - **新增目录**: 
    - 通过顶部"+"按钮或右键菜单添加新目录项
    - 支持添加父级和子级目录
  - **编辑目录**: 通过右键菜单编辑目录标题
  - **删除目录**: 通过右键菜单删除目录项
  - **拖拽排序**: 编辑页支持拖拽调整目录顺序（同级排序 / 跨级升降，最多两级；预览页只读）
  - **右键菜单**: 提供新建、编辑、删除等操作
- **目录交互**:
  - 点击目录项加载并展示该节点对应的 Markdown 内容（见 2.4 数据管理）
  - 章节内锚点跳转 / 滚动定位（待实现）
  - 目录增删改后触发变更事件，实际落库保存逻辑（待实现）
- **返回功能**: 顶部提供返回按钮，可返回文档列表

#### 2.3 Markdown 编辑器
- **编辑器功能**:
  - 实时编辑 Markdown 格式文档
  - 支持撤销/重做操作
  - 代码块语法高亮（基于 `highlight.js`，见 6.6）
- **工具栏功能**（基础能力已具备，体验与缺口见 6.6）:
  - 标题插入（H1-H6）
  - 文本格式化（粗体、斜体、删除线、行内代码）
  - 文本对齐（左/中/右/两端，通过 HTML 标签实现）
  - 列表插入（有序列表、无序列表、任务列表）
  - 引用块插入
  - 代码块插入（预览侧带语法高亮，基于 `highlight.js`）
  - 链接插入（弹窗填写显示文本与地址；选中文本/URL 可预填；地址可自动补全 https://）
  - 图片插入（弹窗支持本地上传或外链；上传复用后端本地文件存储）
  - 水平线插入
  - 表格插入（工具栏插入模板；预览支持 GFM 表格渲染与对齐）
  - 保存、编辑 / 分屏预览 / 纯预览（「全屏预览」指编辑器内仅预览模式，非浏览器全屏）
- **编辑器特性**:
  - 自适应高度
  - 占位符提示
  - 支持双向数据绑定（v-model）
  - **完成标准**：6.6 所列任务全部完成后，笔记编辑器模块视为完成

#### 2.4 数据管理
- **内容存储粒度**：文档内容并非整篇存储，而是按目录节点（`outlineId`）拆分存储；
  点击某个目录项即拉取该节点对应的一段 Markdown 内容并展示在编辑器中。
- 页面加载时按文档 ID 拉取目录树，并默认加载首个目录节点的内容
- 目录结构（增删改、排序）与节点内容的保存均已接入 Pinia store 与 API 层，
  但真实的持久化接口（写目录结构、写节点内容）尚待后端补全

### 3. 文档预览页面

#### 3.1 页面布局
- **分割面板布局**: 使用 Splitpanes 实现可调整的左右分栏
  - 左侧面板：文档目录（默认 20% 宽度，可调整范围 15%-35%）
  - 右侧面板：文档内容（默认 80% 宽度）
  - 支持拖拽调整面板宽度

#### 3.2 文档目录（只读模式）
- 树形结构展示文档目录
- 支持展开/折叠
- 点击目录项加载对应节点的 Markdown 内容（与编辑页一致的按节点加载模型）
- 章节内锚点跳转 / 滚动定位（待实现）
- 高亮当前激活的目录项

#### 3.3 文档内容展示
- **文档头部信息**:
  - 文档标题（大字号，加粗）
  - 文档作者
  - 更新时间
  - 头部与内容区域分隔线
  - 现状：预览页尚未接入真实文档数据（标题/作者/正文为占位数据），
    根据路由参数 `id` 拉取真实文档信息与目录内容为待实现事项
- **Markdown 渲染**:
  - 自定义 Markdown 渲染函数
  - 支持完整的 Markdown 语法：
    - 标题（H1-H6）
    - 段落文本
    - 粗体、斜体、删除线
    - 行内代码和代码块（带语法高亮样式）
    - 有序列表和无序列表
    - 引用块
    - 链接和图片
    - 水平线
    - 表格（样式支持）
  - 代码块样式：灰色背景、圆角边框、等宽字体
  - 链接样式：蓝色、悬停下划线
  - 响应式图片：自适应宽度
- **内容样式**:
  - 居中布局，最大宽度 900px
  - 合适的行间距和段落间距
  - 标题底部边框
  - 代码块和引用块的视觉区分

#### 3.4 响应式设计
- 桌面端：完整双栏布局
- 平板端：调整内边距和字体大小
- 移动端：进一步优化布局和间距

## 四、数据模型

> 说明：文档内容**不是**整篇存储在 `DocumentItem.content` 上，而是按目录节点
> （`OutlineItem.id`，即 `outlineId`）拆分存储的一段 Markdown。文档元信息、
> 目录结构、节点内容三者是分离的，与数据库设计（`documents` /
> `document_outlines` / `outline_contents`）保持一致。

### 4.1 文档数据结构
```typescript
interface DocumentItem {
  id: string              // 文档唯一标识
  title: string           // 文档标题
  description?: string    // 文档描述
  author?: string         // 文档作者
  createdAt?: string      // 创建时间
  updatedAt?: string      // 更新时间
}
```

### 4.2 目录结构数据
```typescript
interface OutlineItem {
  id: string                // 目录项唯一标识
  title: string             // 目录标题
  parentId?: string | number // 父级目录 ID；顶级目录省略 / 为空
  children?: OutlineItem[]  // 子级目录（可选）
  anchor?: string           // 章节锚点（预留，跳转定位待实现）
}
```

### 4.3 节点内容
```typescript
// 按 outlineId 单独获取 / 保存一段 Markdown 内容
type OutlineContent = string
```

- 获取：`getDocumentContent(outlineId)`
- 保存：需补充对应的写接口（当前前端已预留调用位置，后端待实现）

## 五、路由设计

- `/` - 文档列表页面
- `/documents/:id?/edit` - 文档编辑页面（支持新建，id 可选）
- `/documents/:id?/preview` - 文档预览页面（支持新建，id 可选）

## 六、待实现功能

### 6.1 数据持久化（后端对接）
- [ ] 后端 API 真实接入（当前前端基于 Mock 数据运行）
- [ ] 预览页根据路由 `id` 拉取真实文档 / 目录 / 内容（当前为占位数据）
- [ ] 目录结构变更（增删改、排序）落库保存
- [ ] 节点内容（Markdown）保存接口
- [ ] 文档删除接口（前端已调用，Mock 层待补全）

### 6.2 目录跳转功能
- [ ] 编辑页面目录点击跳转到对应章节内锚点并滚动定位
- [ ] 预览页面目录点击跳转到对应章节内锚点并滚动定位
- [ ] 说明：目前点击目录项会切换加载对应节点的内容（已实现），
  但"同一篇内容中按锚点跳转定位"尚未实现

### 6.3 文档设置功能
- [x] 文档基础信息编辑（标题、描述、作者等）— 复用创建弹窗
- [x] 文档删除功能（前端交互已实现，需二次确认）
- [ ] 文档标签管理

### 6.4 目录拖拽排序
- [x] 目录节点拖拽排序（编辑页双层 `vuedraggable` + `PUT .../outlines/reorder`）
  - 同级排序、二级换父、无子一级可降为二级；有子一级禁止降级
  - 拖拽手柄操作，失败回滚；预览页不可拖

### 6.5 其他功能
- [ ] 文档搜索功能
- [ ] 文档分类/标签筛选
- [ ] 文档导出功能（PDF、Markdown 等）
- [ ] 自动保存功能
- [ ] 版本历史记录

### 6.6 Markdown 编辑器收尾（按优先级排序）

> 以下任务全部完成后，笔记编辑器模块视为完成。
> 「全屏预览」按设计保持为编辑器内纯预览模式，不做浏览器全屏。

1. [x] **优化已有工具栏实现体验**
   - [x] 对齐（左/中/右/两端）：预览不再显示原始 `<div align="...">` 标签，按对齐样式渲染
   - [x] 无序/有序/任务列表：点击工具栏在当前行插入标记，光标落在标记后；Enter 延续同类型下一项，空项再 Enter 退出列表
   - [x] 任务列表、水平线：插入到当前光标位置（修复失焦导致插到顶部）
2. [x] **插入表格** — 预览侧 GFM 表格渲染与样式（表头/分隔行/单元格、左中右对齐）；工具栏在光标处插入模板并选中首个表头
3. [x] **插入链接** — 弹窗输入显示文本与链接地址；选中内容预填；地址校验与 https 补全；确认后插入到原光标/选区
4. [x] **插入图片** — 弹窗支持「上传图片」与「图片外链」两种方式；上传复用后端本地文件存储能力
   - 后端新增 `POST /api/notes/v1/upload/image`（notes 模块鉴权），与管理端 `POST /api/admin/v1/upload/image` 共享同一套本地磁盘存储逻辑（`pkg/upload`），文件仍落盘在 `upload.dir/images/{yyyyMMdd}/{随机名}.ext`，经 `/uploads` 静态路由对外提供访问
   - 笔记内容中始终保存站点相对路径（如 `/uploads/images/xxx.png`），便于跨环境迁移；仅在渲染展示（编辑器预览 / 只读预览）时按需解析为可访问地址：前后端同域部署时原样使用，跨域部署（`VITE_API_URL` 为绝对地址）时自动补全为后端源
   - 部署持久化：`docker-compose.yml` 为后端容器的 `/app/uploads` 挂载命名卷 `note-uploads`，避免容器重建导致图片丢失；`docker/frontend/nginx.conf` 新增 `/uploads` 反向代理（转发到 `backend:8080`），供前后端同域部署时使用
   - 编辑区支持直接粘贴（截图/复制的图片）或拖拽本地图片文件：自动上传并原地插入正确的图片 Markdown，避免浏览器把剪贴板文件名当纯文本插入导致预览区出现无法渲染的 `!文件名` 之类原始文本
5. [x] **代码块语法高亮** — 基于 `highlight.js`（按需注册常用语言，未标注语言时自动检测），预览侧渲染带颜色的代码块

## 七、UI/UX 特性

### 7.1 设计风格
- 简洁现代的界面设计
- 绿色主题色（`--el-color-primary` / `--el-color-success`，当前取值 `#5a9e58`，
  相较早期方案更柔和不刺眼）
- 品牌 Logo + 标题并排展示于列表页头部
- 卡片式布局
- 柔和的阴影和过渡效果

### 7.2 交互体验
- 流畅的页面过渡动画
- 悬停效果反馈
- 响应式设计，适配多端
- 直观的操作流程

### 7.3 可访问性
- 清晰的视觉层次
- 合理的字体大小和行间距
- 良好的对比度
- 键盘导航支持（部分功能）