# 知识笔记软件 — 数据库设计

> 数据库：PostgreSQL 16  
> 字符集：UTF-8  
> 时间字段统一使用 `TIMESTAMPTZ`  
> 主键策略：`BIGSERIAL`（业务 API 可序列化为字符串，与前端 `id: string` 对齐）

---

## 一、设计目标

1. 支撑文档列表、目录树、按目录节点存储 Markdown 内容。
2. 预留多用户能力：所有权落在 `documents.user_id`，后续加登录无需改表结构。
3. 目录 / 内容不冗余存 `user_id`，通过文档归属鉴权，避免数据不一致。
4. MVP 可 seed 一个默认用户；个人使用阶段接口可固定使用该用户。

---

## 二、ER 关系

```text
users 1 ───< documents 1 ───< document_outlines 1 ─── 1 outline_contents
                              │
                              └── parent_id（自关联树）
```

| 关系 | 说明 |
|------|------|
| users → documents | 一对多；文档必须属于某用户 |
| documents → document_outlines | 一对多；删文档级联删目录 |
| document_outlines → document_outlines | 自关联；`parent_id` 为空表示顶级 |
| document_outlines → outline_contents | 一对一；`outline_id` 即主键 |

---

## 三、表结构

### 3.1 `users` — 用户表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGSERIAL | PK | 用户 ID |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 登录名 |
| password_hash | VARCHAR(255) | NOT NULL | 密码哈希（bcrypt 等） |
| display_name | VARCHAR(50) | NOT NULL | 展示名；创建文档时可作默认 author |
| status | SMALLINT | NOT NULL, DEFAULT 1 | 1 正常 / 0 禁用 |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT NOW() | 更新时间（触发器维护） |

**索引**

- `uq_users_username`：`UNIQUE (username)`
- `idx_users_status`：`(status)`

**说明**

- 与模板中的 `admin_users` 职责分离；笔记业务统一使用本表。
- MVP：migration / seed 写入一个默认用户（如 `admin`）。

---

### 3.2 `documents` — 文档表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGSERIAL | PK | 文档 ID |
| user_id | BIGINT | NOT NULL, FK → users(id) | 所属用户（所有权根） |
| title | VARCHAR(50) | NOT NULL | 文档标题（1–50 字符，应用层校验） |
| description | VARCHAR(200) | NULL | 文档描述（最多 200 字符） |
| author | VARCHAR(20) | NOT NULL | 展示作者；可与 `display_name` 不同 |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT NOW() | 更新时间（触发器维护） |

**外键**

- `fk_documents_user`：`user_id REFERENCES users(id)`  
  - 删除用户：建议 `ON DELETE RESTRICT`（先清理文档再删用户）；个人库若希望省事可用 `CASCADE`。

**索引**

- `idx_documents_user_updated`：`(user_id, updated_at DESC)` — 按用户拉列表
- `idx_documents_user_id`：`(user_id)` — 若复合索引已覆盖可省略

**与前端对齐**

```typescript
interface DocumentItem {
  id: string
  title: string
  description: string
  author: string
  createdAt?: string
  updatedAt?: string
}
```

`user_id` 由后端从登录态写入，前端不传，防止越权。

---

### 3.3 `document_outlines` — 文档目录表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGSERIAL | PK | 目录节点 ID |
| document_id | BIGINT | NOT NULL, FK → documents(id) | 所属文档 |
| parent_id | BIGINT | NULL, FK → document_outlines(id) | 父节点；NULL 表示顶级（不用 `'0'`） |
| title | VARCHAR(100) | NOT NULL | 目录标题 |
| sort_order | INT | NOT NULL, DEFAULT 0 | 同级排序，越小越靠前 |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT NOW() | 创建时间 |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT NOW() | 更新时间（触发器维护） |

**外键**

- `fk_outlines_document`：`document_id REFERENCES documents(id) ON DELETE CASCADE`
- `fk_outlines_parent`：`parent_id REFERENCES document_outlines(id) ON DELETE CASCADE`  
  - 删父节点时级联删子节点（及对应内容，见下表）

**索引**

- `idx_outlines_document_parent_sort`：`(document_id, parent_id, sort_order)` — 拉树 / 排序
- `idx_outlines_document_id`：`(document_id)` — 按文档批量查询

**说明**

- **不设 `user_id`**：鉴权路径为 `outline → document.user_id`。
- API 组装树时把 `parent_id IS NULL` 的节点作为根，填充 `children`。
- 前端历史 mock 里 `parentId: '0'` 表示顶级；对接时后端用 `null`，响应里可映射为省略或 `null`。

**与前端对齐**

```typescript
interface OutlineItem {
  id: string
  title: string
  parentId?: string | null  // 顶级为 null / 不传
  children?: OutlineItem[]
}
```

---

### 3.4 `outline_contents` — 目录节点内容表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| outline_id | BIGINT | PK, FK → document_outlines(id) | 与目录 1:1，**不再单独设 id** |
| content | TEXT | NOT NULL, DEFAULT '' | Markdown 正文 |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT NOW() | 更新时间（触发器维护） |

**外键**

- `fk_contents_outline`：`outline_id REFERENCES document_outlines(id) ON DELETE CASCADE`

**说明**

- 主键即 `outline_id`，与前端 `getDocumentContent(outlineId)` 一致。
- 新建目录节点时可同时插入一行空内容，或首次保存时再插入（二选一，建议创建节点时插入空行，读写路径统一）。
- 版本历史若以后需要，另建 `outline_content_versions`，本表仍保持 1:1 当前稿。

---

## 四、公共约定

### 4.1 `updated_at` 触发器

复用 / 与现有 migration 一致的函数：

```sql
CREATE OR REPLACE FUNCTION set_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

对 `users`、`documents`、`document_outlines`、`outline_contents` 分别建立 `BEFORE UPDATE` 触发器。

### 4.2 软删除

MVP **不做软删除**；删除为物理删除 + 级联。若后续需要回收站，再为 `documents` 增加 `deleted_at`。

### 4.3 所有权校验（应用层）

| 操作对象 | 校验方式 |
|----------|----------|
| 文档 | `documents.user_id = 当前用户` |
| 目录 / 内容 | JOIN `documents`，校验 `user_id` |

MVP 未上登录时，中间件可注入 seed 用户的 `user_id`，SQL 仍带用户条件，便于以后接 JWT。

---

## 五、建议 Migration 顺序

| 序号 | 文件（建议名） | 内容 |
|------|----------------|------|
| 001 | （已有）admin_users 等模板表 | 可逐步废弃，与笔记业务脱钩 |
| 002 | `002_create_users_table.sql` | `users` + 触发器 |
| 003 | `003_create_documents_table.sql` | `documents` |
| 004 | `004_create_document_outlines_table.sql` | `document_outlines` |
| 005 | `005_create_outline_contents_table.sql` | `outline_contents` |
| seed | `seed/00x_default_user.sql` | 默认用户 |

---

## 六、API 与表映射（便于开发对照）

| 能力 | 主要涉及表 |
|------|------------|
| 文档列表 / 创建 / 改元信息 / 删除 | `documents`（+ `users`） |
| 拉取目录树 | `document_outlines` |
| 目录增删改 / 排序 | `document_outlines`（删时级联内容） |
| 读 / 写章节 Markdown | `outline_contents` |

内容读写键：`outline_id`。  
列表排序建议：`documents.updated_at DESC`。

---

## 七、后续可扩展（当前不建表）

| 能力 | 建议 |
|------|------|
| 标签 | `tags` + `document_tags` |
| 全文搜索 | `documents` / `outline_contents` 上 GIN（`to_tsvector`）或外部引擎 |
| 版本历史 | `outline_content_versions(outline_id, version, content, created_at)` |
| 多端登录 | 复用 `users`，加 refresh_token / session 表即可 |
